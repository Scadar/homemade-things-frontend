import React, { FC } from "react";
import { Box } from "@mui/material";

const Calendar: FC = () => {

    return (
        <Box>
            Calendar
        </Box>
    );
};

export default Calendar;